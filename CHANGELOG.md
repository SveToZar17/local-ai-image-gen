# Что исправлено

## 🔴 Критические
1. **Добавлен .env.example** — без него проект не запускался (README ссылался на несуществующий файл).
2. **CORS в API** — веб-интерфейс на localhost:8080 теперь может обращаться к API на localhost:8000.
3. **Flux предупреждение** — в README добавлено примечание, что стандартный workflow несовместим с Flux.

## 🟡 Серьёзные
4. **str.format() → str.replace()** — промпты с фигурными скобками больше не ломают сервер.
5. **Thread-safe LLM** — `threading.Lock()` защищает от двойной загрузки модели при параллельных запросах.
6. **Workflow кэшируется** — загружается один раз при старте, а не при каждом запросе.
7. **API healthcheck** — `docker-compose.yml` теперь проверяет, что API действительно поднялся, прежде чем запускать бота и веб.
8. **Telegram Conflict** — бот теперь красиво сообщает, если запущен дубликат.
9. **test_generation.py** — поддерживает переменную окружения `API_BASE_URL` для тестирования по сети.

## 🟢 Мелкие
10. **README: YOUR_USERNAME → SveToZar17** — правильная ссылка на клонирование.
11. **Веб-UI: AbortController** — можно отменить предыдущий запрос, нажав кнопку повторно.
12. **Веб-UI: escapeHtml** — итоговый промпт безопасно вставляется в DOM.
13. **Веб-UI: API_BASE_URL** — корректно работает и с localhost, и с IP в локальной сети.
14. **Безопасная подстановка в workflow** — проверка наличия ключей перед записью.
